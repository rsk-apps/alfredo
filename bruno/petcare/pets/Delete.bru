meta {
  name: Delete Pet
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/pets/:id
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

params:path {
  id: {{petId}}
}
