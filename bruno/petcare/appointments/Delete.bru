meta {
  name: Delete Appointment
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/pets/:id/appointments/:aid
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

params:path {
  id: {{petId}}
  aid: {{appointmentId}}
}
