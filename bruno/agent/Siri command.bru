meta {
  name: Siri command
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/agent/siri
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

body:json {
  {
    "text": "{{agentText}}"
  }
}
