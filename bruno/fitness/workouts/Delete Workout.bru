meta {
  name: Delete Workout
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/fitness/workouts/:id
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

params:path {
  id: {{workoutId}}
}
