meta {
  name: Delete Goal
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/fitness/goals/:id
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

params:path {
  id: {{goalId}}
}
