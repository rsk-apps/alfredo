meta {
  name: Stop Treatment
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/pets/:id/treatments/:tid
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

params:path {
  id: {{petId}}
  tid: {{treatmentId}}
}
